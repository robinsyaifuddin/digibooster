
import React from 'react';

// This is a placeholder component since we've removed the theme toggle functionality
// but some components still reference it
const ThemeToggle = () => {
  return null;
};

export default ThemeToggle;
